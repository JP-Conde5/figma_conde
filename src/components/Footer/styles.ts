import { StyleSheet } from "react-native"

export const styles = StyleSheet.create({
    footer:{
        flex: 0.1,
        marginBottom: 30,
    },

    mushrooms: {
        flexDirection: 'row',
        justifyContent: 'space-around',
        alignItems: 'center',  
        width: 300,
    }
})